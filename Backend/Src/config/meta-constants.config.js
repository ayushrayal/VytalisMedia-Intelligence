/**
 * Constants configuration for Meta / Windsor integrations.
 * Centralizes connector names, URLs, and default parameter values.
 */

const WINDSOR_CONSTANTS = {
  CONNECTOR_FACEBOOK: "facebook",
  BASE_URL: "https://connectors.windsor.ai",
  DEFAULT_DATE_PRESET: "last_7d",
};

module.exports = WINDSOR_CONSTANTS;
